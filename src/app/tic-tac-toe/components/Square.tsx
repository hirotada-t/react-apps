import Button from "./Button";

export default function Square() {
    return (
        <Button onClick={() => { }} className="w-10 h-10 border border-gray-400">
            X
        </Button>
    )
}